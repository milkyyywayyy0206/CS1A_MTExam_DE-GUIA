// array of colors
let colors = [];

for ( let i = 0 ; i < 3 ; i++){
    let color = prompt("Enter a color you Like: ")
    colors.push(color); // push the color or the user input
    console.log(colors); // print the colors array 
}

